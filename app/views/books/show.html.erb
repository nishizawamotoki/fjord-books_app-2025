<p style="color: green"><%= notice %></p>

<%= render @book %>

<div>
  <%= link_to t('books.links.edit'), edit_book_path(@book) %> |
  <%= link_to t('books.links.back_to_list'), books_path %>

  <%= button_to t('books.buttons.destroy'), @book, method: :delete %>
</div>
